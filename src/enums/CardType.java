package src.enums;

import src.logic.models.cards.CrossStrike;
import src.logic.models.cards.QuadShot;
import src.logic.models.cards.SkipTurn;
import src.logic.models.cards.Sonar;

public enum CardType {
	CROSS_STRIKE,
	QUAD_SHOT,
	SKIP_TURN,
	SONAR;
//
//		case SONAR -> new Sonar();
//		case QUAD_SHOT -> new QuadShot();
//		case CROSS_STRIKE -> new CrossStrike();
//		case SKIP_TURN -> new SkipTurn();
}
